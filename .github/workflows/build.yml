name: test_actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - name: checkout
        uses: actions/checkout@v3
        with:
          cc: gcc
          cxx: g++
      - name: build
        run: pwd && ls && ls ../
